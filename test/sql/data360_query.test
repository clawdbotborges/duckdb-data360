# name: test/sql/data360_query.test
# description: data360 query requires a registry-backed oauth_pkce secret
# group: [sql]

require data360

query I
SELECT function_type FROM duckdb_functions() WHERE function_name = 'data360_query';
----
table

statement error
CREATE SECRET forged_data360 (TYPE data360, PROVIDER oauth_pkce);
----
Data 360 OAuth secrets are created by data360_auth_complete

statement error
CREATE PERSISTENT SECRET forbidden_data360 (TYPE data360, PROVIDER oauth_pkce);
----
D360-AUTH-014 PERSISTENCE_FORBIDDEN

statement error
CREATE SECRET process_injection (TYPE data360, PROVIDER process, LOGIN_URL 'https://org.my.salesforce.com');
----
Secret provider 'process' not found for type 'data360'

statement error
SELECT * FROM data360_query('select 1', 'missing_secret');
----
Data 360 secret 'missing_secret' was not found
